module.exports = {
  /** Node types. */
  NODE_TYPE: {
    /** #define keyword. */
    DEFINE: 1,
    /** #include keyword. */
    INCLUDE: 2,
    /** #if keyword. */
    IF: 3,
    /** #ifdef keyword. */
    IFDEF: 4,
    /** #ifndef keyword. */
    IFNDEF: 5,
    /** #else keyword. */
    ELSE: 6,
    /** #elif keyword. */
    ELIF: 7,
    /** #endif keyword. */
    ENDIF: 8,
    /** #dup keyword. */
    DUP: 9,
    /** #enddup keyword. */
    ENDDUP: 10
  }
}